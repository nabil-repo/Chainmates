/**
 * hud.ts
 * In-world HUD system.
 * The primary HUD is rendered crisply via 2D React-DCL UI (ui.tsx).
 * This module maintains lightweight ambient tags without obstructing sightlines.
 */

export function setupHud() {
  // 2D React-DCL UI handles the primary HUD cleanly without world clutter.
}

export function hudSystem(_dt: number) {
  // Handled via mainUpdateSystem & React-DCL UI
}
